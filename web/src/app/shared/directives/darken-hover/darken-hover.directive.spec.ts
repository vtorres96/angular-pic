import { DarkenHoverDirective } from './darken-hover.directive';

describe('DarkenHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new DarkenHoverDirective();
    expect(directive).toBeTruthy();
  });
});
